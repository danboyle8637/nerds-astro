<div class="card">
  <div class="container">
    <p class="pre-head">A website is</p>
    <p class="headline">Not a Business Card</p>
  </div>
</div>

<style lang="scss">
  .card {
    width: 290px;
    perspective: 800px;
  }

  .container {
    padding: 20px;
    display: grid;
    grid-template-columns: 1fr;
    grid-auto-rows: min-content;
    gap: 6px;
    background-color: #161E2D;
    width: 100%;
    transform: rotateX(12deg) rotateY(22deg) rotateZ(-12deg);
  }

  .pre-head {
    font-size: 2.1rem;
    font-weight: 700;
    color: var(--font-color-light);
  }

  .headline {
    font-size: 3rem;
    font-weight: 700;
    color: var(--font-color-light);
  }
</style>